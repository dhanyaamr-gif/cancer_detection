# NovaDx Brain Tumor Detection - Training Dependencies
# =====================================================
# This file lists all Python packages required to build, train, and evaluate
# the brain tumor classification CNN.
#
# Install with:
#   pip install -r requirements.txt
#
# It is recommended to use a virtual environment (e.g., `python -m venv venv`).

# --- Core deep learning framework ---
# TensorFlow provides the Keras API used to build and train the CNN.
tensorflow>=2.12.0

# --- Image processing ---
# OpenCV for image loading, resizing, and augmentation.
opencv-python>=4.8.0

# Pillow for basic image handling and file format support.
Pillow>=10.0.0

# --- Numeric / data handling ---
# NumPy for array operations and numeric computation.
numpy>=1.24.0

# Pandas for loading and manipulating tabular data (e.g., CSVs, metadata).
pandas>=2.0.0

# --- Visualization ---
# Matplotlib for plotting training curves, confusion matrices, and images.
matplotlib>=3.7.0

# --- Machine learning utilities ---
# Scikit-learn for evaluation metrics, train/test splits, and preprocessing.
scikit-learn>=1.3.0

# --- Training progress / logging ---
# tqdm for progress bars during data loading and predictions.
tqdm>=4.65.0

# --- Jupyter notebook support ---
# For exploratory analysis in the notebooks/ directory.
jupyter>=1.0.0
ipykernel>=6.25.0

# --- Optional: TensorBoard for training visualization ---
# TensorBoard is bundled with TensorFlow but pinned here for clarity.
tensorboard>=2.12.0

# --- Optional: DICOM medical imaging support ---
# pydicom for reading DICOM MRI/CT files if used in the dataset pipeline.
pydicom>=2.4.0

# --- Optional: Model visualization ---
# pydot & graphviz help visualize model architecture (requires graphviz binary).
pydot>=1.4.2
